**Switch** — instant-apply on/off toggle (settings, not form fields). Knob slides; track turns violet when on.

```jsx
<Switch label="Auto-print job tickets" defaultChecked />
<Switch label="Notify on art approval" size="sm" />
```

Sizes `sm | md`. For boolean form inputs that submit with a form, prefer `Checkbox`.
