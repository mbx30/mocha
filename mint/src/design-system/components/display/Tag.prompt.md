**Tag** — squared chip for filters and multi-select tokens; quieter than Badge.

```jsx
<Tag color="#863bff">Business cards</Tag>
<Tag onRemove={() => removeFilter('rush')}>Rush only</Tag>
```

Props: `color` (swatch), `icon`, `onRemove` (adds dismiss ✕). For status pills use `Badge` instead.
